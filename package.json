{
  "name": "footy-tipping",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint --dir src --dir scripts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:python": "python3 -m pytest tests/python/",
    "test:live-tips": "node scripts/test_live_tips.mjs",
    "check": "npm run lint && npm run typecheck && npm test",
    "check:all": "npm run check && npm run test:python",
    "archive:snapshot": "ROUND=$(node -e \"const fs=require('fs');const data=JSON.parse(fs.readFileSync('data/current_round_tips.json','utf8'));process.stdout.write(String(data.round ?? ''));\" ) && TIMESTAMP=$(date +%Y-%m-%d) && mkdir -p data/archive && cp data/current_round_tips.json \"data/archive/${TIMESTAMP}_round_${ROUND}.json\" && cp data/current_round_tips.json \"data/archive/round_${ROUND}.json\" && echo \"Archived round ${ROUND}\""
  },
  "dependencies": {
    "next": "^15.5.15",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.13.9",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^6.0.1",
    "@vitest/coverage-v8": "^4.1.5",
    "eslint": "^9.39.4",
    "eslint-config-next": "^15.5.15",
    "jsdom": "^29.0.2",
    "typescript": "^5.8.2",
    "vitest": "^4.1.5"
  }
}
